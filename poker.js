const poker = (player1Cards, player2Cards) => {
    player1Results = {};
    player2Results = {};

    for (const card of player1Cards) {
        numOfCards = {};

        if (numOfCards[card])
    }
}

console.log(poker([1, 1, 12, 12, 7], [2, 2, 3, 4, 5]));